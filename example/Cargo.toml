[package]
name = "example"
version = "0.1.0"
edition = "2024"

[dependencies]
binrw = { path = "../../binrw" }
fast-zip = { package = "rzip", path = ".." }
tempfile = "3.23.0"
tokio = { version = "1.48.0", features = ["full"] }
