[package]
name = "example"
version = "0.1.0"
edition = "2024"

[dependencies]
binrw = { path = "../../binrw" }
fast-zip = { package = "rzip", path = "..",features=["parallel"] }
tempfile = "3.23.0"
tokio = { version = "1.48.0", features = ["full"] }
memmap2 = "0.9.10"
